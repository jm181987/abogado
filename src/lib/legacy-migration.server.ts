/**
 * The one-time legacy migration has been completed and verified in PostgreSQL.
 * This compatibility hook performs no network calls.
 */
export async function ensureLegacyMigration() {
  return;
}
